# El binario de xmrig se descarga solo al ejecutar el launcher
xmrig/
_xmrig_tmp/
*.zip
*.tar.gz
# El .exe compilado se distribuye via GitHub Release, no en git
PROYECTA-Miner.exe
# Scripts temporales de build (empiezan con _)
_*.ps1
